# ----------------------------
# Backend settings
# ----------------------------
DB_PATH=./data/portfolio.db
ADMIN_SESSION_TTL_DAYS=30
SEED_ADMIN_EMAIL=lnieuwenhuis48@icloud.com
SEED_ADMIN_PASSWORD=change-me-strong-password

SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
CONTACT_EMAIL=contact@safasfly.dev

# Comma-separated list of allowed CORS origins
CORS_ORIGINS=http://localhost:5173,https://safasfly.dev,https://www.safasfly.dev,https://staging.safasfly.dev

# ----------------------------
# Frontend build settings
# ----------------------------
# Use /api when frontend and backend are served from the same domain through nginx.
VITE_API_BASE=/api

# Optional: force staging guard on all hosts (otherwise enabled for staging.*)
# VITE_STAGING_GUARD=true
# VITE_STAGING_HOST_PREFIX=staging.
